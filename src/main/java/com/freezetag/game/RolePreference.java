package com.freezetag.game;

/**
 * Represents a player's role preference when joining the queue.
 */
public enum RolePreference {
    /** No preference; random assignment. */
    NONE,
    /** Player prefers to be a Runner. */
    RUNNER,
    /** Player prefers to be a Tagger. */
    TAGGER
}
